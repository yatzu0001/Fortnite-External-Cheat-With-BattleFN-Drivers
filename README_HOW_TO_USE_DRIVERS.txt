# You should have the file DLLHVK.DLL in the folder where u got the external / loader 

## Basically , you should LOAD the FULL bypass before even start the cheat. That's in the folder "BYPASS_DRIVER"

( you can use the file "rundrv.bat" to load the drivers, remember to put "driver.sys" in your C:\ Directory, and change path's to your folder where u stored
 driver , in the cmd file . )

n.b. " when you run the cmd file, the driver with the random name (AJIFHJ24981.sys" will return an error, not a status success. don't worry , thats normal, cause it
call STATUS_ERROR to delete itself.

### then , you just need to run your compiled cheat from software.


#REMINDER#

THIS IS A PoC. BUT is STILL FULLY WORKING. It has added to A PASTED CHEAT , with CLEANED CODE, A BYPASS FROM BATTLEFN CRACKED CHEAT, 


You can implement easily your batch file and the copy on C:\ of the driver.sys , with a simple cpp or c# console app, easily .
